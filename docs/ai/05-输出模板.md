# AI 输出模板

## 推荐结构

### 1. 目标

- 当前目标平台：`<desktop / iOS / Android / all>`
- 当前目标环境：`<localhost / LAN / trusted HTTPS / production>`
- 目标结果：`<install to desktop / add to home screen / trusted local HTTPS / full PWA behavior>`

### 2. 主路径

- 推荐主路径：`<browser / platform flow>`
- 原因：`<why this is the best default>`

### 3. 备选路径

- 备选路径 1：`<fallback>`
- 备选路径 2：`<fallback>`

### 4. 不推荐路径

- 不推荐路径：`<path>`
- 原因：`<brief reason>`

### 5. 安全与信任要求

- 是否要求可信 HTTPS：`<yes / no / conditional>`
- 是否依赖 `service worker`：`<yes / no>`
- 是否需要证书导入：`<yes / no / platform-specific>`

### 6. 平台差异

- 桌面端：`<summary>`
- iOS / iPadOS：`<summary>`
- Android：`<summary>`

### 7. 实现动作

- 需要的网页能力：
  - `manifest.json`
  - `service worker`
  - 安装引导 UI
- 需要的本地脚本或部署动作：`<summary>`

### 8. 风险与限制

- 限制 1：`<brief>`
- 限制 2：`<brief>`

### 9. 最终建议

- 如果是本地调试：`<recommendation>`
- 如果是正式发布：`<recommendation>`
